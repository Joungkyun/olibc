INSTALLATION

$Id$

1. Quick Install
	./configure
	make
	make install

2. Dependent Packages

	1. ICONV
		1-1. glibc > 2.2 (Imbeded iconv)
		1.2. GNU libiconv
	2. libidn >= 1.0.0
	3. pcre >= 4.5

3. Useful Environment on Building

	CFLAGS
	LDFLAGS
	CPPFLAGS

	use DESTDIR on make install:
		make DESTDIR=/var/tmp/foo install
