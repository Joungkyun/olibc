.TH hex2bin(1) 2003-08-31 "Linux Manpage" "OOPS Library's Manual"
.\" Process with
.\" nroff -man hex2bin.1
.\" 2003-09-04 JoungKyun Kim <htt://www.oops.org>
.SH 이름
hex2bin

.SH 사용법
.B #include <olibc/libstring.h>
.sp
.BI "char * hex2bin (char *str);"
.sp
.BI "char * _hex2bin (char c);"

.SH 설명
hex2bin 은 16진수 형태의 문자열을 이진수의 문자열로 반환한다.

_hex2bin 은 캐릭터를 이진수 형태의 문자열로 반환을 한다.


.SH 반환값
이진수 형태의 문자열로 반환. hex2bin 의 반환 값은 꼭 메모리 해제(free)를 해야 한다.

.SH 예제
.nf
#include <olibc/libstring.h>

char *data = "6EA7";
char ret[17];
int i,j;

memset (ret, '\0', sizeof (ret));

for (i=0, j=0; i<strlen(data); i++) {
    memcpy (ret + j, _hex2bin (data[i]), 4);
    j += 4;
}

printf ("%s\\n", ret);
.fi

위의 코드는 hex2bin 의 원형이 된다.

.nf
#include <olibc/libstring.h>

char *data = "6EA7";
char *ret;

ret = hex2bin (data);

printf ("%s\\n", ret);
free (ret);
.fi

.SH 참고
bin2dec(1)

.SH 작성
2003.09.04 김정균 <http://www.oops.org>

.SH 버그
버그가 발견이 되면 http://www.oops.org 로 리포팅을 부탁한다.

.SH 저작권
BPL License v.1
.br
http://devel.oops.org/document/bpl 을 참고 하도록 한다.
